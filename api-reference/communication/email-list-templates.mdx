---
title: "List Templates"
description: "Retrieve all email templates."
api: "GET /v1/email/templates"
---

<CodeGroup>
```bash curl
curl https://api.usenaive.ai/v1/email/templates \
  -H "Authorization: Bearer nv_sk_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.usenaive.ai/v1/email/templates",
    headers={"Authorization": "Bearer nv_sk_your_key_here"},
)
data = response.json()
```

```typescript TypeScript
const response = await fetch("https://api.usenaive.ai/v1/email/templates", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```

```javascript JavaScript
const response = await fetch("https://api.usenaive.ai/v1/email/templates", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```
</CodeGroup>

<ResponseField name="data" type="array">
  Array of template objects.
</ResponseField>

```json Example Response
{
  "data": [
    { "id": "tpl_abc123", "name": "Welcome Email", "status": "published" },
    { "id": "tpl_def456", "name": "Password Reset", "status": "draft" }
  ]
}
```
