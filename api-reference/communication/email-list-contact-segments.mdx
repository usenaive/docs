---
title: "List Contact Segments"
description: "List all segments a contact belongs to."
api: "GET /v1/email/contacts/:contactId/segments"
---

<ParamField path="contactId" type="string" required>
  The contact ID.
</ParamField>

<CodeGroup>
```bash curl
curl https://api.usenaive.ai/v1/email/contacts/ct_abc123/segments \
  -H "Authorization: Bearer nv_sk_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.usenaive.ai/v1/email/contacts/ct_abc123/segments",
    headers={"Authorization": "Bearer nv_sk_your_key_here"},
)
data = response.json()
```

```typescript TypeScript
const response = await fetch("https://api.usenaive.ai/v1/email/contacts/ct_abc123/segments", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```

```javascript JavaScript
const response = await fetch("https://api.usenaive.ai/v1/email/contacts/ct_abc123/segments", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```
</CodeGroup>

<ResponseField name="data" type="array">
  Array of segment objects the contact belongs to.
</ResponseField>

```json Example Response
{
  "data": [
    { "id": "seg_xyz789", "name": "Newsletter Subscribers" },
    { "id": "seg_abc456", "name": "Premium Users" }
  ]
}
```
