---
title: "List Topics"
description: "Retrieve all email subscription topics."
api: "GET /v1/email/topics"
---

<CodeGroup>
```bash curl
curl https://api.usenaive.ai/v1/email/topics \
  -H "Authorization: Bearer nv_sk_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.usenaive.ai/v1/email/topics",
    headers={"Authorization": "Bearer nv_sk_your_key_here"},
)
data = response.json()
```

```typescript TypeScript
const response = await fetch("https://api.usenaive.ai/v1/email/topics", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```

```javascript JavaScript
const response = await fetch("https://api.usenaive.ai/v1/email/topics", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```
</CodeGroup>

<ResponseField name="data" type="array">
  Array of topic objects.
</ResponseField>

```json Example Response
{
  "data": [
    { "id": "top_abc123", "name": "Product Updates" },
    { "id": "top_def456", "name": "Marketing" }
  ]
}
```
