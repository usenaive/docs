---
title: "List Contact Properties"
description: "Retrieve all defined contact properties."
api: "GET /v1/email/contact-properties"
---

<CodeGroup>
```bash curl
curl https://api.usenaive.ai/v1/email/contact-properties \
  -H "Authorization: Bearer nv_sk_your_key_here"
```

```python Python
import requests

response = requests.get(
    "https://api.usenaive.ai/v1/email/contact-properties",
    headers={"Authorization": "Bearer nv_sk_your_key_here"},
)
data = response.json()
```

```typescript TypeScript
const response = await fetch("https://api.usenaive.ai/v1/email/contact-properties", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```

```javascript JavaScript
const response = await fetch("https://api.usenaive.ai/v1/email/contact-properties", {
  headers: { "Authorization": "Bearer nv_sk_your_key_here" },
});
const data = await response.json();
```
</CodeGroup>

<ResponseField name="data" type="array">
  Array of contact property definitions.
</ResponseField>

```json Example Response
{
  "data": [
    { "id": "prop_abc123", "name": "company", "type": "string" },
    { "id": "prop_def456", "name": "plan_tier", "type": "string" }
  ]
}
```
